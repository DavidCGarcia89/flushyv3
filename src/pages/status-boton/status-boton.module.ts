import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { StatusBotonPage } from './status-boton';

@NgModule({
  declarations: [
    StatusBotonPage,
  ],
  imports: [
    IonicPageModule.forChild(StatusBotonPage),
  ],
})
export class StatusBotonPageModule {}
